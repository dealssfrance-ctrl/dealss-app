
  # Design Dealss Mobile App UI (copie)

  This is a code bundle for Design Dealss Mobile App UI (copie). The original project is available at https://www.figma.com/design/QUGPLOL2TcHk1TaG9IeuOR/Design-Dealss-Mobile-App-UI--copie-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  